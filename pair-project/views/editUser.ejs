<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
</head>
<body>
    <h1>Form User Edit</h1>
    <form action="/users/edit/<%= dataUser.id %>" method="post">
        <label for="fullName"> Full Name</label><br>
        <input type="text" name="fullName" value="<%= dataUser.fullName %>"><br>
        <label for="role"> Role</label><br>
        <input type="text" name="role" value="<%= dataUser.role %>"><br>
        <label for="username"> Username</label><br>
        <input type="text" name="username" value="<%= dataUser.username %>"><br>
        <label for="password"> Password</label><br>
        <input type="password" name="password" value="<%= dataUser.password %>" id="myInput" minlength="8"><br>
        <input type="checkbox" onclick="myFunction()"><label>Show Password</label><br>
        <script>
            function myFunction() {
              var x = document.getElementById("myInput");
              if (x.type === "password") {
                x.type = "text";
              } else {
                x.type = "password";
              }
            }
            </script>
        <button type="submit">Save User</button>
        <button> <a href="/users">Cancel</a></button>
    </form>
</body>
</html>